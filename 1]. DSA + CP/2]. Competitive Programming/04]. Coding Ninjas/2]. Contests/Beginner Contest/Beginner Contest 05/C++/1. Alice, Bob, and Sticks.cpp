/*
    Time complexity: O(1)
    Space complexity: O(1)
*/

int stickWays(int n) {
    // Calculate the number of possible pairs.
    int ans = (n - 1) / 2;

    // Return the possible pairs.
    return ans;
}
